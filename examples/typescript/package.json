{
  "name": "mosaic-connect-claude-sdk-example-ts",
  "version": "1.0.0",
  "description": "Mosaic Connect + Claude minimal example agent (TypeScript)",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "ts-node agent.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.95.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
